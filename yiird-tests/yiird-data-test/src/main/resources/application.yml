spring:
  autoconfigure:
    exclude:
  profiles:
    active: single
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        cache:
          use_second_level_cache: false
logging:
  level:
    root: info